apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.yml -o /home/hojaldre/repos/E-Vote-GitOps/
    kompose.version: 1.38.0 (HEAD)
  labels:
    io.kompose.service: audit-service
  name: audit-service
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: audit-service
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker-compose.yml -o /home/hojaldre/repos/E-Vote-GitOps/
        kompose.version: 1.38.0 (HEAD)
      labels:
        io.kompose.service: audit-service
    spec:
      activeDeadlineSeconds: 120
      initContainers:
        - name: wait-for-db
          image: busybox
          command:
            - sh
            - -c
            - until (echo > /dev/tcp/postgres-audit/5432) 2>/dev/null; do echo "waiting for postgres-audit..."; sleep 2; done
      containers:
        - args:
            - uvicorn
            - main:app
            - --host
            - 0.0.0.0
            - --port
            - "3003"
          envFrom:
            - configMapRef:
                name: evote-env
            - secretRef:
                name: evote-secrets
          env:
            - name: DB_HOST
              value: "postgres-audit"
            - name: DB_PORT
              value: "5432"
            - name: DB_USER
              value: "audit_service_user"
            - name: DB_NAME
              value: "audit_db"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: evote-secrets
                  key: AUDIT_DB_PASSWORD
          image: ghcr.io/ricardoandaluzsinisterra/evote-audit-service:main-dcbf07a
          livenessProbe:
            exec:
              command:
                - curl
                - -f
                - http://localhost:3003/health
            failureThreshold: 3
            periodSeconds: 30
            timeoutSeconds: 5
          name: audit-service
          ports:
            - containerPort: 3003
              protocol: TCP
      imagePullSecrets:
        - name: ghcr-secret
      restartPolicy: Always
